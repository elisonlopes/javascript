function parimpar(n){ // AS funções possuem: Chamada, ação, parâmetro e retorno
    if (n%2 == 0){ // if e else é a ação
        return 'PAR' // PAR ou IMPAR são o retorno
    }else{
        return'IMPAR'
    }

}

console.log(parimpar(1358)) // PARIMPAR é a chamda (1358) é o parâmetro